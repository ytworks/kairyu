"""Checkout-only benchmark entrypoints.

The modules in this package are stable path/module compatibility wrappers for
developer benchmarks and formal evidence operators.  Reusable, installed
benchmark code belongs in :mod:`kairyu.bench`; this package is deliberately
excluded from the Kairyu wheel.
"""
