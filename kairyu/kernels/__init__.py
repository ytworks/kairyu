"""GPU kernels (deploy-day verified): Triton GEMMs over the m14 quant formats.

Everything in this package is coverage-omitted and deferred-import; the CPU
dequant references in kairyu/quant are the correctness oracles
(tests/gpu/test_quant_kernels.py).
"""
